<%= form_for(current_user.following_relationships.find_by(followed_id: follow_user_id),
      html: { method: :delete },
      remote: true) do |f| %>
  <%= f.submit t('common.follow.following'), class: "btn" %>
      
<% end %>