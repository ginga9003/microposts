    <%= form_for(current_user.user_retweets.build, 
        html: { id: "retweet_form_" + micropost.id.to_s }, 
        remote: true) do |f| %>
      <%= hidden_field_tag :micropost_id, micropost.id %>
      <span class="glyphicon glyphicon-retweet" data-micropost="<%= micropost.id %>"<% if current_user == micropost.user %> style="visibility: hidden;"<% end %>></span>
    <% end %>