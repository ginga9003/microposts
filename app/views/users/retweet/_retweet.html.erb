    <%= form_for(current_user.user_retweets.build, 
        html: { id: "retweet_form_" + micropost.id.to_s }, 
        remote: true) do |f| %>
      <%= hidden_field_tag :micropost_id, micropost.id %>
      <span class="retweet_area" onclick="execRetweet(<%= micropost.id %>);"<% if current_user == micropost.user && retweet == 0 %> style="visibility: hidden;"<% end %>>
        <span class="glyphicon glyphicon-retweet"></span>
        <% if micropost.micropost_retweet_users.length != 0 %>
        <span><%= micropost.micropost_retweet_users.length %></span>
        <% end %>
      </span>
    <% end %>